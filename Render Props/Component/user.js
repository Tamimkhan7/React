

export default function user({ render }) {

    // jokhn amar function asbe tokhn function akare receive korte hobe otherwise normally receive korle hobe
    // return name(true);
    //just change my function name that's work as same when my funciton name is 'name' now render
    return render(true);
}